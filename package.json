{
  "name": "atchyut.dev",
  "version": "0.1.0",
  "lockfileVersion": 3,
  "requires": true,
  "packageManager": "yarn@4.0.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "yarn workspace portfolio dev",
    "web:build": "yarn workspace portfolio build",
    "web:start": "yarn workspace portfolio start",
    "web:lint": "yarn workspace portfolio lint",
    "web:test": "yarn workspace portfolio test",
    "blog": "yarn workspace blog dev",
    "blog:build": "yarn workspace blog build",
    "blog:start": "yarn workspace blog start",
    "blog:lint": "yarn workspace blog lint",
    "blog:test": "yarn workspace blog test",
    "clean": "rm -rf node_modules"
  }
}
